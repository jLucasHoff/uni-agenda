import {json} from '@sveltejs/kit';

//detecta se o curso existe, se sim, redireciona para a página (na página da agenda, se vier daqui ok, se não tem que buscar o post também)
export const POST = async ({request, cookies}:{request: Request, cookies: any}) => {
}

//pega os dados do curso
export const GET = async ({request, cookies}:{request: Request, cookies: any}) => {
    
}